{
  "name": "casetracker",
  "version": "0.1.0",
  "private": true,
  "description": "Hackathon prototype — Challenge 3: Supporting casework decisions",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest --coverage",
    "lint": "eslint src/"
  },
  "dependencies": {
    "express": "^4.19.2",
    "nunjucks": "^3.2.4"
  },
  "devDependencies": {
    "@eslint/js": "^9.x",
    "eslint": "^9.x",
    "jest": "^29.x",
    "nodemon": "^3.x",
    "pa11y": "^8.x",
    "wait-on": "^8.x"
  },
  "engines": {
    "node": ">=20"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/src/__tests__/**/*.test.js"]
  }
}
